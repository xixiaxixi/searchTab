{
  "manifest_version": 3,
  "name": "Search New Tab",
  "version": "0.3",
  "description": "Replaces the default new tab page with a custom one",
  "permissions": [
    "tabs",
    "storage",
    "bookmarks",
    "history"
  ],
  "chrome_url_overrides": {
    "newtab": "index.html"
  },
  "i18n": {
    "default_locale": "zh_CN",
    "locales": {
      "en": {
        "description": "Replaces the default new tab page with a custom one"
      },
      "zh_CN": {
        "description": "替换默认的新标签页为自定义页面"
      }
    }
  }
}
